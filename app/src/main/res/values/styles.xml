<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--recyclerView分割线-->
        <item name="android:listDivider">@drawable/divider_bg</item>
    </style>


    <!--自定义对话框-->
    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 是否qu标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <!--自定义对话框-->
    <style name="WriteDialog" parent="android:style/Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <!--<item name="android:windowBackground">@color/transparent</item>-->
        <!-- 是否qu标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>


    <style name="DialogTransparent" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>


    <style name="RadioButtonGreen" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/SyllabusBottomGreen</item>
        <item name="colorControlActivated">@color/SyllabusBottomGreen</item>
    </style>


</resources>
